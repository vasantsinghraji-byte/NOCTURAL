global:
  resolve_timeout: 5m

route:
  receiver: platform-oncall
  group_by: ['alertname', 'component', 'severity']
  group_wait: 30s
  group_interval: 5m
  repeat_interval: 4h
  routes:
    - matchers:
        - component="security"
        - severity="critical"
      receiver: security-critical
      repeat_interval: 30m
      continue: true
    - matchers:
        - component="security"
      receiver: security-oncall
      repeat_interval: 1h
    - matchers:
        - severity="critical"
      receiver: platform-critical
      repeat_interval: 30m

inhibit_rules:
  - source_matchers:
      - severity="critical"
    target_matchers:
      - severity="warning"
    equal: ['alertname', 'component']

receivers:
  - name: platform-oncall
    webhook_configs:
      - url: '${ALERTMANAGER_PLATFORM_WEBHOOK_URL}'
        send_resolved: true
    slack_configs:
      - api_url: '${ALERTMANAGER_PLATFORM_SLACK_WEBHOOK_URL}'
        title: '{{ template "nocturnal.title" . }}'
        text: '{{ template "nocturnal.text" . }}'
        send_resolved: true

  - name: platform-critical
    webhook_configs:
      - url: '${ALERTMANAGER_PLATFORM_CRITICAL_WEBHOOK_URL}'
        send_resolved: true
    slack_configs:
      - api_url: '${ALERTMANAGER_PLATFORM_CRITICAL_SLACK_WEBHOOK_URL}'
        title: '{{ template "nocturnal.title" . }}'
        text: '{{ template "nocturnal.text" . }}'
        send_resolved: true

  - name: security-oncall
    webhook_configs:
      - url: '${ALERTMANAGER_SECURITY_WEBHOOK_URL}'
        send_resolved: true
    slack_configs:
      - api_url: '${ALERTMANAGER_SECURITY_SLACK_WEBHOOK_URL}'
        title: '{{ template "nocturnal.title" . }}'
        text: '{{ template "nocturnal.security.text" . }}'
        send_resolved: true

  - name: security-critical
    webhook_configs:
      - url: '${ALERTMANAGER_SECURITY_CRITICAL_WEBHOOK_URL}'
        send_resolved: true
    slack_configs:
      - api_url: '${ALERTMANAGER_SECURITY_CRITICAL_SLACK_WEBHOOK_URL}'
        title: '{{ template "nocturnal.title" . }}'
        text: '{{ template "nocturnal.security.text" . }}'
        send_resolved: true

templates:
  - '/etc/alertmanager/templates/*.tmpl'
